sss

# 部署到 GitHub Pages
node deploy.js github -t ghp_xxx -r username/repo -p ./dist

# 部署到 Netlify
node deploy.js netlify -t netlify_xxx -n my-site -p ./dist

# 部署到 Vercel
node deploy.js vercel -t vercel_xxx -n my-project -p ./dist

# 部署到腾讯云 CloudBase
node deploy.js cloudbase -e env-xxx --secret-id AKIDxxx --secret-key xxx -p ./dist

# 部署到 tebi.io
node deploy.js tebi -b my-bucket -a AKIDxxx -s xxx -p ./dist


# GitHub
DEPLOY_TOKEN=github_pat_xxx
GITHUB_REPO=username/repo
GITHUB_BRANCH=gh-pages

# Netlify
NETLIFY_TOKEN=xxx
NETLIFY_SITE_ID=xxx

# Vercel
VERCEL_TOKEN=xxx
VERCEL_PROJECT_NAME=my-site

# CloudBase
CLOUDBASE_ENV_ID=env-xxx
CLOUDBASE_SECRET_ID=AKIDxxx
CLOUDBASE_SECRET_KEY=xxx

# tebi.io
TEBI_BUCKET=my-bucket
TEBI_ACCESS_KEY=xxx
TEBI_SECRET_KEY=xxx


# GitHub Pages
npm install @actions/github

# Netlify (需要 archiver)
npm install archiver

# 腾讯云 CloudBase
npm install @cloudbase/node-sdk

# tebi.io
npm install aws-sdk

# 通用
npm install axios form-data glob


# 1. 安装依赖
npm install axios form-data glob @actions/github archiver @cloudbase/node-sdk aws-sdk

# 2. 创建配置文件 .deploy-config.json

# 3. 运行（会部署到所有 enabled: true 的平台）
node deploy.js








